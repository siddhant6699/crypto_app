import 'package:flutter/material.dart';

class CryptoView extends StatelessWidget {
  const CryptoView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Crypto Eye'),
      ),
      body: Text('Hello'),
    );
  }
}
